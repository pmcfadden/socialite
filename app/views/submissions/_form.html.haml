= form_for @submission do |f|
  -if @submission.errors.any?
    #error_explanation
      %h2= "#{pluralize(@submission.errors.count, "error")} prohibited this submission from being saved:"
      %ul
        - @submission.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    %br
    = f.text_field :title
  .field
    = f.label :url
    .explanation
      Leave this blank if you just want to start a conversation.
    = f.text_field :url
  .field
    = f.label :description
    %br
    = f.text_area :description
  .actions
    = f.submit 'Submit story'
