%span
  = "posted by "
  = link_to user, :controller => :users, :action => :profile, :id => user.id
  %span{:class => "karma-indicator"}= "[#{user.karma}]"
  = " - #{time_ago_in_words_including_ago(resource.created_at)}"
