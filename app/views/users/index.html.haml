%h1 Listing users

%table
  %tr
    %th Username
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= link_to 'Edit', edit_user_path(user)
      %td
        = link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete
        %span= "(destroy is not fully implemented)"
